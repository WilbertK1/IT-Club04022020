#include <iostream>

using namespace std;

int main () {
	// relational and comparison operators
	// ==, !=, >, < >=, <=
	bool test = (7 < 4); // false
	cout << test << endl; // prints 0
	cout <<
	// not only numeric can be be compared
	int a = 3, b = 8;
	cout << (a == b) << endl;
	cout << (a * b >= 20) <<  endl;
	return 0;
}
